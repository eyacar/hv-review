name: Deploy

on:
  push:
    branches: [main]

# TODO: replace the placeholder job below with your hosting provider steps.
# `npm run build` produces a static bundle in /dist — compatible with
# Amplify, Cloudflare Pages, Vercel, Netlify, etc.

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder — no deploy target configured yet
        run: echo "Deploy target not configured. Wire up your provider here."
