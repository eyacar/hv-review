name: Deploy

on:
  push:
    branches: [main]

# Placeholder — wire up to your static hosting provider here.
# vite build produces a static bundle in /dist, compatible with
# any provider (Amplify, Cloudflare Pages, Vercel, etc.)
#
# Example for AWS Amplify (add secrets in repo settings):
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm
#       - run: npm ci
#       - run: npm run build
#       - name: Deploy to Amplify
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: us-east-1
